import { TOAST_STRINGS } from "./toastStrings";
import { COMMON_STRINGS } from "./commonStrings";
import { COORDINATES } from "./coordinates";
import { DB_STRINGS } from "./dbStrings";
import { EMPTY_STRINGS } from "./emptyStrings";
import { HEADING_STRINGS } from "./headingStrings";
import { SOCKET_STRINGS } from "./socketStrings";

export {
  TOAST_STRINGS,
  COMMON_STRINGS,
  COORDINATES,
  DB_STRINGS,
  EMPTY_STRINGS,
  HEADING_STRINGS,
  SOCKET_STRINGS,
};
